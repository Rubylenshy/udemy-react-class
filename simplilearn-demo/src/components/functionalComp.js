import React from 'react'

function functionalComp() {
    return(<div>
            <h1>Functional Components</h1>
            <p>Components in general have to be nested into the main components
                 using the <em>import</em> and <em>export</em> keywords</p>
        </div>);
}

export default functionalComp;